<p style="color: green"><%= notice %></p>

<%= render @project %>

<div class="display-8 text-center">
  <div class="btn btn-secondary btn-sm my-1"><%= link_to "Edit this project", edit_project_path(@project), :class => "btn" %></div>
  <div class="btn btn-secondary btn-sm my-1"><%= link_to "Back to projects", projects_path, :class => "btn" %></div>

  <%= button_to "Destroy this project", @project, method: :delete %>
</div>
